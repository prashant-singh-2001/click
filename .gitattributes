# Normalize line endings: LF in the repo, auto on checkout.
* text=auto eol=lf

# Keep Windows batch/PowerShell scripts as CRLF where it matters.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binary assets — never normalize.
*.png binary
*.ico binary
*.icns binary
*.lnk binary
